python3 update.py && python3 -m bot && 
mv web/psa /usr/local/bin/psa &&
chmod +x /usr/local/bin/psa &&
mv web/pahe /usr/local/bin/pahe &&
chmod +x /usr/local/bin/pahe &&
mv web/up /usr/local/bin/up &&
chmod +x /usr/local/bin/up &&
mv web/mkv /usr/local/bin/mkv &&
chmod +x /usr/local/bin/mkv &&
mv web/ma /usr/local/bin/ma &&
chmod +x /usr/local/bin/ma &&
mv web/toon /usr/local/bin/toon &&
chmod +x /usr/local/bin/toon &&
mv web/pmz /usr/local/bin/pmz &&
chmod +x /usr/local/bin/pmz &&
mv web/ola /usr/local/bin/ola &&
chmod +x /usr/local/bin/ola &&
mv web/htp /usr/local/bin/htp &&
chmod +x /usr/local/bin/htp &&
mv web/0 /usr/local/bin/0 &&
chmod +x /usr/local/bin/0 &&
rm config.env &&
rm Dockerfile 
